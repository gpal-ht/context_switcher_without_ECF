# Vendor Dependencies

This folder contains versioned engineering dependencies used by this project.

## ecf

The Engineering Control Framework provides reusable engineering process, standards, workflows, roles, and quality gates.

Context Switcher may consume a bundled version of ECF when running engineering workflows.

The bundled dependency must be treated as read-only.