# Vendor Dependencies

This folder contains versioned framework dependencies used by this repository.

## engineering_kb

The Engineering Knowledge Base provides reusable engineering knowledge.

ECF may consume a bundled version of EKB when running engineering reasoning workflows.

The bundled dependency must be treated as read-only.